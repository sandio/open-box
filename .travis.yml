language: ruby
cache: bundler

before_install:
  - mkdir -p $HOME/bin
  - curl -s https://testspace-client.s3.amazonaws.com/testspace-linux-dev.tgz | tar -zxvf- -C $HOME/bin
  - testspace config url http://095d8acfaaf71b4a0f3767e41152620ebdda1b31@mark.stridespace.com

install:
  - echo "install .."

matrix:
   include:
   - rvm: 2.2.3
     gemfile: Gemfile.rails40
     env: J=ONE
   - rvm: 2.3.3
     gemfile: Gemfile.rails41
     env: J=TWO

#rvm:
#- 2.2.3
#- 2.3.3

#gemfile:
# - Gemfile.rails40
# - Gemfile.rails41
 
#env:
#  matrix:
#   - JOB=ABC
#   - JOB=DEF
  
script:
  - echo "script .."
  - printenv > printenv.txt
 
after_script:
  - testspace ["$J - $TRAVIS_JOB_NUMBER"]test.xml "+.travis.yml{build script}" "+printenv.txt{environment variables}" 